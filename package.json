{
  "name": "eduai",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "share": "npm run build && concurrently \"npm run start -w server\" \"npx --yes localtunnel --port 3001\""
  },
  "workspaces": ["server", "client"],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
